import TextButton from './TextButton';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(TextButton);

metadata.exportInfo = {
  path: 'src/TextButton/TextButton.js',
};
