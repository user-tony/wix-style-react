import TimeInput from './TimeInput';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(TimeInput);

metadata.exportedFrom({
  path: 'src/TimeInput/TimeInput.js',
});
