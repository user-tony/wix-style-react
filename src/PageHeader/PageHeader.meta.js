import PageHeader from './PageHeader';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(PageHeader);

metadata.exportedFrom({
  path: 'src/PageHeader/PageHeader.js',
});
