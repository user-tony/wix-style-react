import FileUpload from './FileUpload';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(FileUpload);

metadata.exportedFrom({
  path: 'src/FileUpload/FileUpload.js',
});
