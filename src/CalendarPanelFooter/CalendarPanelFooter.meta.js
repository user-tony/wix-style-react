import CalendarPanelFooter from './CalendarPanelFooter';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(CalendarPanelFooter);

metadata.exportInfo = {
  path: 'src/CalendarPanelFooter/CalendarPanelFooter.js',
};
