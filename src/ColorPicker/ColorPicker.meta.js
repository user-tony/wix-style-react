import ColorPicker from './ColorPicker';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(ColorPicker);

metadata.exportInfo = {
  path: 'src/ColorPicker/ColorPicker.js',
};
