import GenericModalLayout from './GenericModalLayout';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(GenericModalLayout);

metadata.exportInfo = {
  path: 'src/GenericModalLayout/GenericModalLayout.js',
};
