import BadgeSelect from './BadgeSelect';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(BadgeSelect);

metadata.exportedFrom({
  path: 'src/BadgeSelect/BadgeSelect.js',
});
