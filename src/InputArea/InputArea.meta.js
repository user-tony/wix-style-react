import InputArea from './InputArea';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(InputArea);

metadata.exportedFrom({
  path: 'src/InputArea/InputArea.js',
});
