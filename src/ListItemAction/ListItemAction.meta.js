import { ListItemAction } from './ListItemAction';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(ListItemAction);

metadata.exportInfo = {
  path: 'src/ListItemAction/ListItemAction.js',
};
