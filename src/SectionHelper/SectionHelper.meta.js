import SectionHelper from './SectionHelper';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(SectionHelper);

metadata.exportInfo = {
  path: 'src/SectionHelper/SectionHelper.js',
};
