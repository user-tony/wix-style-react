# Single Area Drag and Drop Grid

An example how to use the `<SortableGrid/>` component to create reorderable grid of items.

The items can be dragged completely or using only a handler, give the configuration.
