import { TableToolbar } from './TableToolbar';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(TableToolbar);

metadata.exportedFrom({
  path: 'src/TableToolbar/TableToolbar.js',
});
