import SidebarBackButton from './SidebarBackButton';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(SidebarBackButton);

metadata.exportInfo = {
  path: 'src/SidebarBackButton/SidebarBackButton.js',
};
