import LinearProgressBar from './LinearProgressBar';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(LinearProgressBar);

metadata.exportInfo = {
  path: 'src/LinearProgressBar/LinearProgressBar.js',
};
