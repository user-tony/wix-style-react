import Swatches from './Swatches';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(Swatches);

metadata.exportInfo = {
  path: 'src/Swatches/Swatches.js',
};
