import BarChart from './BarChart';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(BarChart);

metadata.exportInfo = {
  path: 'src/BarChart/BarChart.js',
};
