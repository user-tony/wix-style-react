import MultiSelect from './MultiSelect';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(MultiSelect);

metadata.exportInfo = {
  path: 'src/MultiSelect/MultiSelect.js',
};
