import IconButton from './IconButton';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(IconButton);

metadata.exportInfo = {
  path: 'src/IconButton/IconButton.js',
};
