import DropdownBase from './DropdownBase';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(DropdownBase);

metadata.exportedFrom({
  path: 'src/DropdownBase/DropdownBase.js',
});
