import SidebarHeader from './SidebarHeader';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(SidebarHeader);

metadata.exportInfo = {
  path: 'src/SidebarHeader/SidebarHeader.js',
};
