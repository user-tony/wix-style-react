import CalendarPanel from './CalendarPanel';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(CalendarPanel);

metadata.exportInfo = {
  path: 'src/CalendarPanel/CalendarPanel.js',
};
