import InputWithLabel from './InputWithLabel';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(InputWithLabel);

metadata.exportInfo = {
  path: 'src/InputWithLabel/InputWithLabel.js',
};
