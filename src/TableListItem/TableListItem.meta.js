import TableListItem from './TableListItem';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(TableListItem);

metadata.exportedFrom({
  path: 'src/TableListItem/TableListItem.js',
});
