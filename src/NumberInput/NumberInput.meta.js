import NumberInput from './NumberInput';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(NumberInput);

metadata.exportedFrom({
  path: 'src/NumberInput/NumberInput.js',
});
