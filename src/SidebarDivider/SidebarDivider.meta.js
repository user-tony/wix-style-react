import SidebarDivider from './SidebarDivider';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(SidebarDivider);

metadata.exportInfo = {
  path: 'src/SidebarDivider/SidebarDivider.js',
};
