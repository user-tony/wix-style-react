import ListItemEditable from './ListItemEditable';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(ListItemEditable);

metadata.exportInfo = {
  path: 'src/ListItemEditable/ListItemEditable.js',
};
