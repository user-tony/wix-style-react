import Collapse from './Collapse';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(Collapse);

metadata.exportInfo = {
  path: 'src/Collapse/Collapse.js',
};
