import Carousel from './Carousel';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(Carousel);

metadata.exportInfo = {
  path: 'src/Carousel/Carousel.js',
};
