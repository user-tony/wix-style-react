import ThemeProvider from './ThemeProvider';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(ThemeProvider);

metadata.exportedFrom({
  path: 'src/ThemeProvider/ThemeProvider.js',
});
