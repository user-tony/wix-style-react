import Divider from './Divider';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(Divider);

metadata.exportInfo = {
  path: 'src/Divider/Divider.js',
};
