import CloseButton from './CloseButton';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(CloseButton);

metadata.exportInfo = {
  path: 'src/CloseButton/CloseButton.js',
};
