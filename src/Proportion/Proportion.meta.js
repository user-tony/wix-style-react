import Proportion from './Proportion';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(Proportion);

metadata.exportInfo = {
  path: 'src/Proportion/Proportion.js',
};
