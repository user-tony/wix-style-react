import NoBorderInput from './NoBorderInput';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(NoBorderInput);

metadata.exportInfo = {
  path: 'src/NoBorderInput/NoBorderInput.js',
};
